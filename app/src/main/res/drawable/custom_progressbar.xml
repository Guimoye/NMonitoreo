<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/bg_progress_color"
                android:centerColor="@color/bg_progress_color"
                android:centerY="1.0"
                android:endColor="@color/bg_progress_color"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/progress_color"
                    android:centerColor="@color/progress_color"
                    android:centerY="1.0"
                    android:endColor="@color/progress_color"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>