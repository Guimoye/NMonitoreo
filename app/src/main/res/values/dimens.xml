<resources>

    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>

    <!-- Margenes de componentes -->
    <dimen name="component_top_margin">3dp</dimen>
    <dimen name="component_right_margin">0dp</dimen>
    <dimen name="component_bottom_margin">0dp</dimen>
    <dimen name="component_left_margin">0dp</dimen>

    <!-- Boton codigo de barra-->
    <dimen name="scan_btn_width">40dp</dimen>
    <dimen name="photo_btn_width">200dp</dimen>

    <!--imagen de la firma-->
    <dimen name="firma_width">300dp</dimen>
    <dimen name="firma_height">160dp</dimen>

    <!-- Boton expandible -->
    <dimen name="span_byn_width">50dp</dimen>

    <!-- Boton de fecha - hora -->
    <dimen name="date_btn_width">120dp</dimen>

    <!-- Pading header seccion -->
    <dimen name="padding_header">5dp</dimen>
    <dimen name="header_top_margin">10dp</dimen>
    <!-- Pading componente seccion -->
    <dimen name="padding_component">4dp</dimen>

    <!-- Height Estatico -->
    <dimen name="height_default">40dp</dimen>

    <!-- Margen entre componentes -->
    <dimen name="margin_component">4dp</dimen>

    <!-- Altura de audios -->
    <dimen name="height_audio">80dp</dimen>

    <!-- Padding en textos -->
    <dimen name="padding_text">2dp</dimen>

    <!-- Margen de imagen de marker (con prioridad)-->
    <dimen name="margin_marker_top">20dp</dimen>

    <dimen name="padding">16dp</dimen>
    <dimen name="text_size">32sp</dimen>
    <dimen name="text_size_servicio">16sp</dimen>
    <dimen name="text_size2">14sp</dimen>
    <dimen name="initial_size">50sp</dimen>

</resources>
